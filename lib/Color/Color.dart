import 'package:flutter/material.dart';

// Paleta de colores consistente
const Color primaryOrange = Color(0xFFF57C00);
const Color lightOrange = Color(0xFFFF9800);
const Color grisClaro = Color(0xFFF5F5F5);
const Color colorFondo = Color(0xFFF5F5F5);
const Color colorTextoPrincipal = Colors.black;
const Color colorTextoSecundario = Colors.grey;

// Colores para Dashboard/Reportes
const Color primaryBlue = Color(0xFF2196F3); // equivalente a Colors.blue
const Color primaryGreen = Color(0xFF4CAF50); // equivalente a Colors.green
const Color primaryRed = Color(0xFFF44336); // equivalente a Colors.red

// Colores configurables de estado (para Dashboard: Próximos y Proyectos)
// Cambia estos dos para ajustar rápidamente la apariencia sin tocar varias pantallas.
const Color statusActiveColor =
    Color(0xFFE88A25); // proyectos activos / en curso
const Color statusCompletedColor = primaryGreen; // proyectos completados

// Fondos Oscuros
const Color colorFondoOscuro = Color(0xFF2C2C2E);
const Color colorFondoMasOscuro = Color(0xFF1D1D1F);
const Color colorFondoCtaFinal = Color(0xFF111111);

// Colores de Mockups
const Color colorMarcoMockup = Color(0xFF1C1C1E);
const Color colorBordeMockup =
    Color(0xFF424242); // Equivalente a Colors.grey[850]

// Texto en Fondos Oscuros
const Color colorTextoOscuro = Colors.white;
